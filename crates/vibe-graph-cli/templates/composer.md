# {{ project.name }}

> Generated by vibe-graph CLI
> Repositories: {{ project.repositories.len() }}
> Total files: {{ project.total_sources() }}

---

{% for repo in project.repositories %}
## {{ repo.name }}

**URL:** `{{ repo.url }}`
**Local Path:** `{{ repo.local_path.display() }}`
**Files:** {{ repo.sources.len() }}

{% for source in repo.sources %}
### {{ repo.name }}/{{ source.relative_path }}

| Property | Value |
|----------|-------|
| Path | `{{ source.relative_path }}` |
| Format | {{ source.format }} |
| Size | {{ source.human_size() }} |

{% match source.content %}
{% when Some with (val) %}
```
{{ val }}
```
{% when None %}
*Content not included (file too large or binary)*
{% endmatch %}

{% endfor %}
{% endfor %}
