# cargo-release configuration
# Run: cargo release patch|minor|major -p vibe-graph-cli

# Pre-release checks
pre-release-commit-message = "chore(cli): release {{version}}"
tag-message = "v{{version}}"
tag-name = "v{{version}}"

# Push to git and publish
push = true
publish = true
tag = true
