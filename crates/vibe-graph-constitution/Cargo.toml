[package]
name = "vibe-graph-constitution"
version = "0.1.0"
edition.workspace = true
description = "Governance and planning constraints for Vibe-Graph"
publish = false

[package.metadata.release]
release = false

[dependencies]
vibe-graph-core = { path = "../vibe-graph-core" }
