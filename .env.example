# Vibe-Graph CLI Configuration
# Copy this file to .env and fill in your values

# GitHub Personal Access Token (required for org commands)
# Create one at: https://github.com/settings/tokens
# Required scopes: repo (for private repos), read:org
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# GitHub username (required for authentication)
GITHUB_USERNAME=your-username

# Default organization to operate on (optional)
GITHUB_ORGANIZATION=your-org

# Cache directory for cloned repositories (optional)
# Defaults to system cache directory
# VG_CACHE_DIR=/path/to/cache

# Output directory for composed files (optional)
# Defaults to current directory
# VG_OUTPUT_DIR=./output

# Maximum file size in KB to include content in output (optional)
# Files larger than this will show metadata only
# VG_MAX_CONTENT_SIZE_KB=50
